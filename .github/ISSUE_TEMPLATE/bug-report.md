---
name: Bug report
about: An engine bug — a script, the study app, or the gate misbehaving.
title: "[bug] "
labels: ["bug"]
---

<!-- Engine bugs only. If this is about how a course run went, use the Course-run report
     template instead. -->

**What happened.**

**Repro.**

<!-- Exact steps or command, and which surface: a `scripts/*.mjs` CLI, the study app, or a gate
     step. -->

**Expected.**

**Environment.**

<!-- OS, Node version (`node -v`), engine commit (`git rev-parse --short HEAD`). -->

**Gate output.**

<!-- The failing step from: typecheck / lint / format:check / validate / test / build. -->

```
paste the failing command and its output
```
